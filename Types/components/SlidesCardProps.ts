/**
 * Props interface for the SlidesCard component
 */
export interface SlidesCardProps {
  /** Unique identifier for the slides card */
  id: string;
  /** Title to display on the card */
  title: string;
  /** Optional link to the presentation */
  link: string;
}